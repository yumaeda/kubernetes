apiVersion: v1
kind: Secret
metadata:
  name: sakabas-nextjs-secrets
  namespace: sakabas-nextjs
  labels:
    app: sakabas-nextjs
type: Opaque
# Apply secrets using: kubectl apply -f secrets.yaml
# Replace placeholder values with actual secrets
# Do NOT commit actual secrets to version control
stringData:
  DATABASE_URL: "postgresql://user:password@sakabas-api:5432/sakabas"
