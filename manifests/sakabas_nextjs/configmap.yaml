apiVersion: v1
kind: ConfigMap
metadata:
  name: sakabas-nextjs-config
  namespace: sakabas-nextjs
  labels:
    app: sakabas-nextjs
data:
  NODE_ENV: "production"
  PORT: "3000"
  # Add any config values that don't need to be secrets
  # NEXT_PUBLIC_API_URL: "https://api.your-domain.com"
